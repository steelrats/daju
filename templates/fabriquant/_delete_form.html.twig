<form method="post" action="{{ path('app_fabriquant_delete', {'id': fabriquant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fabriquant.id) }}">
    <button class="btn">Delete</button>
</form>
